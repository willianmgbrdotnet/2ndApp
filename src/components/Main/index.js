//A main servirá para estilizar a área do Form
import React from "react"
import { View } from "react-native"
import Form from "../Form";

export default function Main(){
    return(
        <View>
            <Form/>
        </View>
    );
}